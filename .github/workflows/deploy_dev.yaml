name: Symfony CI

on:
    push:
        branches: [develop]

jobs:
    ci:
        runs-on: ubuntu-latest

        services:
            postgres:
                image: postgres:15
                env:
                    POSTGRES_PASSWORD: root
                    POSTGRES_USER: root
                    POSTGRES_DB: app_test
                ports:
                    - 5432:5432
                options: >-
                    --health-cmd="pg_isready -U root"
                    --health-interval=10s
                    --health-timeout=5s
                    --health-retries=5

        steps:
            - uses: actions/checkout@v4

            - name: Setup PHP
              uses: shivammathur/setup-php@v2
              with:
                  php-version: 8.2
                  extensions: intl, pdo_pgsql

            - name: Install dependencies
              run: composer install --no-interaction --prefer-dist --no-progress

            - name: Set test env
              run: echo "DATABASE_URL=postgresql://root:root@127.0.0.1:5432/app_test" > .env.test.local

            - name: Cache clear
              run: php bin/console cache:clear --env=test

            - name: Migrations
              run: php bin/console doctrine:migrations:migrate --no-interaction --env=test

            - name: Tests
              run: php bin/phpunit
